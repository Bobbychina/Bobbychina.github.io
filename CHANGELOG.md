# 更新日志 · bobbychina.github.io

这个仓库是 <https://bobbychina.github.io/> 的门面：主页（项目卡片 / 游戏厅 / 关于）、游戏厅 `/games/`
（账号系统 + 游戏本体）、隐藏彩蛋 `/secret/`，以及 Cloudflare Functions（账号与云存档的轻后端）。

- **站点本身**的改动（改版、i18n、账号、彩蛋、图标…）记在这份文件里。
- **游戏本体**（《丧尸末日生存》v4.0「余烬」）逐里程碑的细节在游戏仓库：
  <https://github.com/Bobbychina/zombie-survival/blob/main/CHANGELOG.md> —— 本站游戏厅里的页面就是它的单文件构建产物。

> 口径：日期取那次改动的提交日；`Mxx` 是游戏里程碑编号（与游戏仓库一致）。
> 2026-09-12 的改版重写过公开仓库历史（把 AI 日记挪去私有仓库），所以更早的提交在 `git log` 里已经看不到了。

## 2026-09

| 日期 | 类型 | 内容 |
|---|---|---|
| 09-25 | 游戏 | **《吸血鬼幸存者》主角模型重做（去掉会跳的手）+ 第二关绿色装饰减量**（`/games/vampire-survivors/`，站长反馈两条）：① **行走姿态怪 → 重做模型**：病根是斗篷外侧各有一块 2×2 的"手"，四帧里在 13~15 行上下跳，看起来像身子两侧长了会动的肉刺；三种改法（遮住 / 贴边 / 握手于下摆）都不成立 —— 20×24 尺寸下横伸的前臂没有空间画成连续手臂，贴在外面就永远是独立方块。这一版**直接不再外露手**，走路靠腿的左右摆动 + 渲染层奇数帧弹跳表现。同时把帽子改成尖顶（帽尖→帽筒→帽檐三段剪影）、腿改成"站立并拢 ↔ 跨步外移"两姿态（迈步帧斗篷下摆让出一格，腿不会插进斗篷）、裤色独立成更暗的一档、走路弹跳 2px → **1px**；四套皮肤调色板同步更新。② **第二关绿色装饰太多**：密度 `decoDensity` 0.72 → **0.50**、网格 `decoCell` 96 → **104**，同时把孢子囊囊体改**苍白酸黄**、柠檬树冠改浅黄绿、酸沼草叶尖带黄（`PAL_ABYSS` 新增 `w` 槽），这一关从"一整片绿"变成"暗橄榄地表 + 黄绿相间点缀"。证据：`vs-probe` **19/19**、`vs-skin-probe` **7/7**、`vs-enemy-check` **8/8**、0 未捕获异常；图集 121 张原地重跑生成器逐字节一致；线上同套探针复测同一组数字。新增 `tools/vs-shot-clean.mjs`（直接调渲染器出图，绕开 DOM 面板压暗，供美术评审）。 |
| 09-25 | 游戏 | **《吸血鬼幸存者》修地面闪烁 + 第二关配色改自然 + 主角去畸形 + 第二关专属怪物**（`/games/vampire-survivors/`，站长反馈四条）：① **走动时地面一直闪**：先查出一个真实的亚像素问题（地面贴图用未取整的相机取模相位、世界实体走 `Math.round`，每帧错开不到 1px）并修掉 —— 相机现在只取整一次，地面/实体/边缘线共用。但量化后发现主因不在这：`+1px 平移的对齐残差 6.675` 比"不对齐直接比"的 5.41 还大，说明**地表纹理本身是逐像素高频高对比噪点**（最亮石子比底色亮近 3 倍），镜头一动每个像素都在跳。重写地表生成：全部换成空间连贯的低/中频周期噪声，小石子改成聚簇两三像素团，去掉逐像素 hash 颗粒。走动时帧间差 **13.18 → 6.52**，图集 87KB → 67KB。② **第二关太鲜艳**：底色 `(44,50,22)` 鲜黄绿 → **`(38,40,25)` 暗橄榄**，酸光点 `(196,226,74)` → `(126,142,62)`，所有点缀色收窄亮度区间；两关现在都是自然土色，靠点缀物区分而不是靠底色扎眼。③ **主角"不像人、畸形"**：病根是两截手臂画在第 2~3 列、斗篷从第 3 列才开始，中间隔一列空隙 → 像浮在身侧的白色方块。三种改法试下来采用"披风角色"做法：双手只在斗篷两侧各露一小截随帧上下摆；另给每条腿加暗色内缝、靴底收窄。四套皮肤各补一个暗色档。④ **两关怪物画风不同**：原来 5 个人形怪全是同一张模板换色，新增 **3 只第二关专属酸蚀怪**（蚀酸腐尸 / 孢蝠 / 腐沼祭司），美术源独立在 `tools/enemy-art.js`；顺带修一个真缺陷 —— `onlyFromLevel` 对**怪物完全无效**（`Levels.allows()` 存在但敌人侧从没调用），已补进 `Enemies.pickType`（实测第一关抽 4000 次 0 命中，第二关 600 秒时三只进入抽取前三）。证据：`vs-probe` **19/19**、新增 `vs-flicker-probe` **4/4**、`vs-skin-probe` **7/7**、`vs-enemy-check` **8/8**、0 未捕获异常；图集 121 张**原地重跑生成器逐字节一致**；线上同套探针复测 **19/19 + 8/8**。 |
| 09-25 | 游戏 | **《吸血鬼幸存者》两张地图重做主题美术 + 主角建模与皮肤系统 + 第二关技能小加强**（`/games/vampire-survivors/`，站长点单"地图太单调、两关没区别、提升角色建模、为后续皮肤功能留接口"）：① **地表**：两关底图从"底色+噪点"的 64×64 换成 128×128 **无缝**主题地表（第一关「血色荒野」焦土锈红 + 裂缝/碎石/灰烬/碎骨；第二关「柠檬深渊」酸沼黄绿 + 酸洼/苔斑/酸光），128 周期同时把可见的网格重复感减半（渲染层按 `img.naturalWidth` 取周期，零代码改动）。② **两关不再共用同一套点缀**（根因：`drawDecorations` 写死一个 `deco` 组，两关当然一模一样）——新增 `C.LEVELS[i].deco/decoCell/decoDensity` → `Levels.deco/decoCell/decoDensity` → 渲染层按关卡取组，取不到自动退回通用组；第一关大格低密度（荒）、第二关小格高密度（挤），各 6~7 件专属物件。③ **主角建模**：16×16 三帧 → **24×24 四帧 × 三方向**，形体补齐帽檐剪影/眼睛/围巾/斗篷金边/双腿，另加**四套皮肤**（紫袍巫女/血袍骑士/霜袍术士/毒沼术士）与开始面板的皮肤选择器，选择写进存档（`Save.playerSkin`，顺带修了 `load()` 把字符串字段用 `isFinite` 判掉的真问题）。④ **美术链可重建**：仓库里那份 `tools/gen-sprites.js` 是**旧版、有损的**（只生成 49 张，直接跑会删掉 Boss/柠檬猪/宠物/经验球/酸液/`ground_l2` 的美术）——已补到 115 张，并把主角/点缀的形状与配色拆成 `tools/player-art.js`、`tools/deco-art.js` 两个源文件，**加皮肤只改一张调色板表**。⑤ **数值**：第二关 `playerDmgMul` ×1.15 → **×1.25**（怪血 ×1.3，等效压力回到约 1.04 倍）。证据：图集在 `games/vampire-survivors/` 下 `node tools/gen-sprites.js` **原地重建逐字节一致**；探针 **19/19**、皮肤专项 **7/7**、性能 **185.9 fps / p50 4.2ms**、0 未捕获异常。 |
| 09-24 | 游戏 | **《极速椭圆》再加 4 条特技赛道（TrackMania 味道）+ 幽灵模式修复**（`/games/racing3d/`）：① **十条赛道**：新增 **跳台峡谷**（4 个跳台，28 m 内抬升 6 m + 平唇 + 断崖，40 m/s 起跳飞约 45 m、60 m/s 约 65 m）、**云端高架**（爬到 20 m 高空绕圈，带 24 m 落差跳台，脚下是谷底平地）、**冰湖竞速**（抓地只有 55%，AI 在冰上不漂移且收窄走线）、**小巷卡丁**（12 m 窄路 + 连续 90° 弯，2 圈缠斗）。引擎为此补了三件事：`def.profile` 自定义高度剖面（跳台/断崖/高架都能造）、**离地物理**（路面掉得比自由落体快就真的飞出去：重力抛物线 + 落地冲击减速/扬尘/抖动，判据看前方 1.5~6 m 的下降需求，平缓土坡不会误判）、`def.groundMode='floor'` + `def.grip`（谷底平地 / 冰面抓地系数，物理与 AI 共用）。② **幽灵模式修复**（站长反馈"无法生效"）：原来只在第一次过起点线时启动录制 → **第 2/3 圈跑出的个人最佳根本存不下来**，现在每圈重新开录；存下新幽灵会明确提示一句；选了榜上没传过幽灵的人会自动退回「跟我的最佳跑」并说明原因。新增**真实驾驶**端到端探针（注入本机幽灵 → 选「我的最佳」→ 发车 → 过线 → 断言幽灵模型可见且位置在推进）。十条赛道 × 全套自检全绿，Worker 单测 111 项全绿。 |
| 09-24 | 游戏 | **《极速椭圆》幽灵模式 + 车型差异化 + AI v3**（`/games/racing3d/`）：① **幽灵车**：开场卡片可选「不跟幽灵跑 / 我的最佳 / 官方榜前 3 名」，玩家每圈按 10 Hz 采样 `(t,x,z,yaw)` 存成本地幽灵（一圈约 12 KB，编解码往返误差 ≤5 mm），成绩上榜成功后顺手 `POST /api/ghost` 上传，别人就能跟你的幽灵跑；云幽灵 `GET /api/ghost?game=racing3d&track=&name=` **公开只读、不需要登录**，服务端只保留更快的那条（`kept:false` 不覆盖）+ 30 秒冷却 + 每 IP 限流（Worker 单测 **111 项全绿**）。结算页会告诉你「比幽灵快 x.xx 秒 —— 那时候我跑得真快」。② **车型差异化**：9 款车各有一套物理规格（动力/极速/抓地/转向/漂移倍率），实测 0→100 km/h 方程式 2.58s < 肌肉 2.68s < 轿跑 3.14s < 皮卡 3.55s，峰值侧向 方程式 18.94 > 轿跑 15.52 > 肌肉 14.28 m/s²（车身尺寸包线仍统一，换车即换手感）。③ **AI v3**：会预判护栏（`lat + vLat×0.35s`）、前视距离按曲率收缩、超车一次定边不来回摆、"冷胎期"起步 5 秒不抢线（实测三连回头弯首个弯的挤撞从 3 台降到 0~1 台）、按本车抓地缩放节奏；三档平均单圈 54.17 / **49.39** / 48.40 s，硬核档平均反超参考司机 **1.00 s/圈**。6 条赛道 × 完整比赛自检全绿，线上探针 **28/28**。 |
| 09-24 | 游戏 | **《极速椭圆》漂移不再掉速 + 打完比赛能回主菜单**（`/games/racing3d/`，站长反馈"漂移像踩刹车""跑完只能刷新页面换图"）：① **漂移保速**：漂移附加阻力 1.1→**0** m/s²，并新增"保速回补"（速度掉到入漂速度以下时按 2.5 m/s² 往上补，**上限就是入漂速度**，所以不是加速器）；实测最直直道按住漂移 1.4 s 的保速比 **108.7% → 112.0%**，与抓地直行持平；② **赛后回主菜单**：结算卡片与暂停卡片各加一个「🏁 返回主菜单 (M)」按钮，键盘 `M` 同样可用，`backToMenu()` 完整复位（重摆车阵 / 计时清零 / 清空名次 / 重建相机与选择器）→ 换赛道、换车、改难度都不用刷新页面；③ AI 漂移门槛跟着放宽（半径 <45 m → **<75 m**），标准档与参考司机的差距 **1.95 → 1.15 s/圈**，硬核档平均反超 **1.43 s/圈**。新增常驻自检 **测试 5b**（冲线→结算→回主菜单→换图→再发车跑 20 秒，逐项断言）与诊断脚本 `tests\diag-drift.js`（漂移纵向受力逐帧对账）；6 条赛道 × 完整比赛自检全绿，线上探针 23/23。 |
| 09-23 | 游戏 | **《极速椭圆》手感重做 + AI 变强**（`/games/racing3d/`）：① **手感**从"卡丁车式秒转"改成有重量感的真车 —— 加速 15→11.5 m/s²（0→100 km/h 3.15 s）、刹车 24→12 m/s²（100→0 刹车距离 30 m）、转向速率随速度衰减、车头转动改为渐进建立（二阶滤波）、轮胎侧向力改成"逐渐侧滑"；② **AI** 会**切弯心**（每条赛道预算出一条走线偏移线）并在弯里**按漂移键**，另加**三档难度开关**（开场卡片可选 休闲 / 标准 / 硬核，`localStorage` 记住，只影响 AI）：三档平均单圈 **53.09 / 50.71 / 49.50 s**（硬核档平均已反超参考司机 0.14 s/圈）；③ AI 走线统一按"离路缘留边"夹紧 + 卡住自动倒车脱困（连续 3 次救不回就摆回路面），6 条赛道 × 完整比赛自检全绿（脱困判据改为"连续低速 >6 s 才算趴窝"）。量化体检：`node tests\feel.js`（15 项手感指标）与 `node tests\feel.js --ai --diff=easy|normal|hard`。 |
| 09-23 | 游戏 | **《极速椭圆 · 3D 赛车》上线游戏厅**（`/games/racing3d/`）：纯 Three.js 单文件 3D 竞速，**6 条赛道**（椭圆极速环 / 三连回头弯 / WRC 越野土山 / 城市街道 / 沙漠沙丘 / 雪山盘山）× **9 款车型**，5 台 AI 对手，QQ 飞车式两档漂移（轻点贴弯 / 长按甩尾），泥土路扬尘与漂移胎痕。地形跟随路面的高度场（起伏赛道的地面不再把赛道埋进贴图里）、内侧护坡双重取负的老 bug 一并修掉。同步脚本：游戏仓库 `racing3d/tools/sync-site.mjs`（注入 `/games/auth-config.js`、`/games/account.js`、`/mac-block.js`，本机那份仍是自包含单文件）。 |
| 09-23 | 账号 | **全站排行榜扩到第二个游戏**：Worker `/api/score` 支持 `racing3d`，**按赛道分榜**（`lb:racing3d:<track>`）、按**单圈升序**排名（越小越好，与生存榜的降序相反），字段 `{track, lap, total, speed}`（毫秒整数），赛道 id 用正则 `[a-z0-9_-]{2,18}` 限形；冷却闸改成"每个榜各 60 秒"。口径不变：**看榜不用登录**（GET 公开只读）、**上榜要云账号会话**，响应里只有账号名 + 成绩数字。生存榜口径零改动（worker 单测 92 项全绿，含 18 项新赛车榜用例）。 |
| 09-23 | 站点 | **服务端拒绝 macOS（真 403，不再是客户端软拦）**：站长要求"不要软拦截，直接服务端拒绝连接"，于是把拦截搬到我们自己控制的边缘 —— Cloudflare Pages 的 `functions/_middleware.js` 在**静态资源之前**就返回 403（页面与游戏资源一个字节不下发，响应头带 `x-blocked-platform: macos`、`Vary: User-Agent, sec-ch-ua-platform`）。判定：`sec-ch-ua-platform: macOS`（Chromium）优先、UA 里 `Macintosh|Mac OS X`（Safari）兜底，**iPad 桌面模式（UA 也写 Macintosh，但带 `Mobile/`）不误伤**。顺带修掉 `functions/[[path]].js` 的一个真问题：它对一切非 POST 直接 405，于是 `bobbychina-games.pages.dev` 整站**只有中继能通、静态页面全是 405**（既当不了镜像站、也就没法在边缘拦平台）—— 改成"只管 `/api/score` 与 `/oauth/*`，其余交回静态资源"。部署：`npx wrangler pages deploy . --project-name=bobbychina-games --branch main`。证据：`tools/edge-block-check.ps1` **11/11**（Windows 首页/游戏厅/两个游戏页/静态脚本 200、Mac Safari 首页与游戏页与 API 403、Mac Chrome 平台头 403、iPad 200、403 必带 `x-blocked-platform`）+ 本地 wrangler Pages 模拟器上先跑过同一套 curl 矩阵。⚠️ 边界：主域 `bobbychina.github.io` 是 GitHub Pages，**没有任何服务端钩子**，那一份只能保留客户端拦截（`/mac-block.js`，探针 8/8），要"只有服务端说了算"得把入口换成 Cloudflare 域名或自有域名 |
| 09-23 | 站点 | **全站拒绝 macOS 访问**（站长决定：实测 Mac 上的浏览器在本站游戏里存在安全隐患 + 兼容问题）：新增 `/mac-block.js`，命中后给 `<html>` 挂 `data-mac-block`、藏掉 `<body>`、盖一层整屏说明页（HTTP 403 风格，中英双语：为什么拦、请换哪个平台、自己的 UA 与判定依据、开 issue 的入口），并置 `window.__MAC_BLOCKED__` 供探针验收。判定用 `navigator.userAgentData.platform` 优先、UA 兜底，**iPad 桌面模式（UA 伪装成 Macintosh，但 `maxTouchPoints>1`）不误伤**。覆盖首页 / 游戏厅 / OAuth 回跳页 / 两个游戏页 / 彩蛋两页，脚本同步且紧跟 `<meta charset>`（游戏页的脚本都是 defer / type=module，所以它一定先跑）。⚠️ 两个工程细节：① 《丧尸末日生存》是构建产物、每次同步都被覆盖，所以那条注入写在游戏仓库 `tools/sync-site.mjs` 里，否则下次同步就把拦截弄丢；② GitHub Pages 没有服务端，这属于**客户端拦截**（补强版见上一行：Cloudflare 那边已经是真 403）。证据：`tools/mac-block-probe.mjs` **8/8**（Windows 不拦 / Mac 拦（含文案五项）/ 四个入口都拦得住 / iPad 例外 / 0 未捕获异常），截图见会话归档 |
| 09-12 | 改版 | 主页改成公开版：只留 项目 / 游戏厅 / 关于；AI 日记迁往私有仓库 `bobbychina-diary` |
| 09-12 | 账号 | 游戏厅账号支持云后端（Cloudflare Worker + KV）；隐私说明如实区分「云账号 / 本机账号」 |
| 09-14 | 账号 | 账号=GitHub（设备码 + 令牌码，取消注册流程）；同步游戏的 M23 |
| 09-14 | 彩蛋 | 隐藏彩蛋《站长失踪案》上线（`/secret/`），并补齐内联 favicon（消除 404） |
| 09-15 | 彩蛋 | 彩蛋第二层《站长室》：音频双通道隐写 + MT19937 状态恢复 + 7×2 列置换保险箱 |
| 09-15 | 站点 | 游戏厅新增 **Special Thanks** 鸣谢区（名单数组化，方便继续加人） |
| 09-15 | 游戏 | M29 同步：存档全链路加密；游戏厅随之下线「导出 / 导入存档」入口 |
| 09-16 | 站点 | 主页 + 游戏厅中英双语（独立词典文件 + 切换器） |
| 09-16 | 站点 | favicon / 标签页图标补齐：主页与 `/games/`、登录回跳页同一套配色 |
| 09-20 | 游戏 | **M66 建筑内部地图**：进楼不再是一下搜刮 —— 3~6 间的平面图，房间各有战利品、有锁门（撬棍 / 楼门钥匙 / 硬踹）、有埋伏，一栋楼可以分两趟搜完 |
| 09-20 | 游戏 | **M67 楼内行动日志**：在建筑里也能看见自己搜到了什么（平面图自带一份日志，不再被全屏覆盖层挡住） |
| 09-22 | 游戏 | **M68 身体状态接进每个动作**：手臂受伤搜刮产出下降（最低仍能翻到东西）、头部受伤视野少一圈；人体页与 HUD 直接写明这条伤让你损失了什么。同时把「硬核化路线图」（对标塔科夫/RLCraft/MC 的差距审计与里程碑映射）收进游戏仓库 `docs/DESIGN-HARDCORE.md` |
| 09-22 | 游戏 | **M71 商人系统（塔科夫式）**：好感度 + 忠诚档位 LL1~4（档位决定能买什么、每档多 5%/10%/16% 折扣）+ 委托解锁特定道具（防化服要办过 2 张委托、狙击穿甲弹要军需官线 + 办过 4 张）；第二个商人「军需官」要先架无线电；买卖小涨好感、完成委托 +25、委托过期 -12 |
| 09-23 | 游戏 | **M73 技能硬门槛 + 死亡扣进度**（硬核化线：不追数值更难，追"计划失败的代价"）：① **高级武器/防具要战斗技能** —— 重近战（≥34 伤害，如消防斧）要近战 Lv.3，枪按伤害分两档（≥40 如霰弹枪要射击 Lv.3、≥55 如精准步枪要 Lv.5），重甲（护甲 ≥5 的防弹衣 / 减伤 ≥30% 的防化服）要体能 Lv.3；不够 = **装不上**，但**东西不丢、低一档永远能用**，背包页直接显示「🔒 需要「射击」Lv.5（现在 Lv.1）」且按钮是禁用态（不是点一下才知道）。② **军事管制区 / 实验室要「生存」Lv.3 / Lv.5**：不够则搜刮效率**腰斩**（产出**下限 1 份**，绝不锁死 —— 永远有路走，只是更贵），日志写明需要几级 / 现在几级 / 打几折。③ **死亡清空当前等级进度条**（**等级保留**），弹窗与日志都写清丢了哪几条、多少点。技能页每条技能下面也直接列出它卡着哪些门槛。判定只有一份实现（`src/v4/gate-core.ts`），原来的区域搜刮与大世界 POI 搜刮两条路共用；单测 **870/870**、探针 **10/10**（本地 + 线上，端到端真搜刮 **4 份 vs 8 份** = 腰斩；死亡清 44 点进度而等级保留），回归 `_m57` / `_m68` / `_m72` / `_m72b` 全绿。(750427) |
| 09-23 | 站点 | 《吸血鬼幸存者》**「尸潮之王」二次加强 + 三件轮椅削弱 + 玩家闪避**（站长点名："要那种需要手法 + 配队思路的才能打死"）。**Boss**：血量 3200→4000 基础值（5:00 首次登场乘难度曲线 ×2.45 ≈**9800**，后面每只再 ×1.45）、四档（压制 / 召唤 / 暴怒 / 狂暴）、**七招** —— 新增**激光横扫**（一条 520 长的射线从一侧扫到另一侧，只能横向闪）与**落石**（脚下连点 6 处、延迟炸开，逼你不能站桩），原有环形/扇形/螺旋/召唤/冲撞全面加码（22 发环、7 发扇、弹速 +12%、冲撞 380 速 48 伤）；**护盾阶段**（核心的"配队思路"）：血量跌破 62% / 30% 时 Boss 无敌并召唤一波 8~10 只小弟，**必须清掉才破盾**，破盾后 2.5 秒**虚弱期伤害 ×1.35** —— 纯单体构筑在这一阶段干瞪眼，纯 AoE 又清得慢；血量跌破 18% 进入**狂暴**（冷却 ×0.55、速度 ×1.35、弹伤 ×1.25）。**玩家侧**：新增 <kbd>Shift</kbd> **闪避**（900 速 / 0.16 秒 / 0.3 秒无敌帧 / 1.5 秒冷却）—— 弹幕、激光、冲撞都能躲，这是"手法"的入口。**轮椅削弱**：腐化光环（满级 ≈90→52 dps、半径 -15%、节拍放慢）、环绕骨刃（刃数 6→4、单刀 -28%、同一敌人再命中间隔 0.4→0.55 秒，≈101→53 dps）、血爆新星（伤害 -20%、间隔 +30%）。**血量不是拍的是量的 —— 而且量了四轮，三个坑全在秤上**：给探针加了"实际落到 Boss 身上的伤害"统计 + 缠斗约束（机器人原本一路逃到弹幕外）。前三次数字（117~203 / 196~203 / 337 DPS）全都不可信：① 标签页不在前台时 `requestAnimationFrame` 被浏览器节流，而伤害按帧结算 → 帧率减半 DPS 也减半（改用 CDP 焦点模拟 + 把帧率一起量出来）；② 卡片的六个乘区（伤害/攻速/范围/弹数/幸运/暴击）**跨局残留**，只重置伤害攻速时同一套构筑实测能差一倍；③ god 模式在打 Boss 期间还在自动选卡升级，60 秒后 DPS 自己从 120 跳到 200+（测量期间冻结升级）。清干净后的真数字：中期构筑（bolt Lv5 + 光环 Lv4、零被动、60fps）**≈121 DPS → 9800 血 TTK 81 秒**（本地实测，逐 10 秒打点确认全程恒定）。满构筑（5:00 时 Lv20+）约 3~5 倍 DPS → 20~30 秒，这是设计意图：Boss 的威胁在**机制**（护盾逼你清小弟、激光/落石逼你动），不是血条。探针 **11/11**（含 513 只怪 + 弹幕落石齐发 60fps、狂暴、护盾破盾、闪避无敌帧）；顺手硬化了线上两条假红（rAF 节流导致"前摇计时器不走"、Boss 停在取景框外导致"弹幕画了 0 个"） |
| 09-23 | 站点 | 《吸血鬼幸存者》**平衡性调整**（先建测量台，再改数值）：新增 `tools/vs-balance.mjs` —— 页内挂一个"会跑会躲会选卡"的机器人玩家，吐出一整局的等级/血量/场上怪数/伤害来源。**改前实测**：60 秒才 Lv1、105 秒 Lv4、2:00 顶到 520 硬上限、128 秒必死。三处根因与改法：① 经验石只在 82px 内吸，而玩家边退边打 → 杀完就走的经验全丢在原地 ⇒ 新增"200px 内慢慢飘向玩家"（`GEM_DRIFT_*`）+ 吸附半径 82→120；② 尸潮段 `spawnMul 0.40 × batchMul 2.80` 叠上 `MIN_INTERVAL 0.15` 夹取，**实际 ≈53 只/秒** ⇒ 夹取抬到 0.34、`BATCH_GROWTH 0.021→0.012`、`MAX_BATCH 15→11`、尸潮段改 0.95/1.55（2:00 ≈8.8 只/秒）；③ 被围住时每秒吃 12~15 点、100 血没有成长 ⇒ `MAX_HP 100→120`、`REGEN 0.7→1.0`、`INVULN 0.7→1.0`。**改后实测**：60 秒 Lv5 / 90 秒 Lv9 / 2:00 Lv12（血量首次被压到 73%）/ 4:00 Lv29、场上 95~120 只、全程不死、5:00 Boss 正常登场；探针 16/16 + 12/12、帧率 60.2fps 未回归。另加 `tools/fix-notes-quotes.mjs`（留言板裸引号自检/修复，我在这上面栽过三次） |
| 09-22 | 站点 | 《吸血鬼幸存者》**「尸潮之王」加强**（站长点名：原来只有"走过来撞人"、没机制）：三档血量（压制 100%→66% / 召唤 66%→33% / 暴怒 33%→0）+ 五招 —— **环形弹幕 / 扇形瞄准弹 / 螺旋弹幕 / 召唤小弟 / 冲撞**，每招都有**前摇光环**（颜色区分：绿黄紫橙红；前摇期间它站住不动，看到光环就知道躲哪招）；弹幕上限 260、小弟上限 14（保性能）。数值全在 `config.js` 的 `C.BOSS.PHASES / MOVES`，逻辑在新 `js/game/bosskit.js`；弹幕用**预渲染小球贴图**（不在绘制里新建渐变，守住性能红线）。新增 `tools/vs-boss-probe.mjs` **12/12**（招式轮转 / 前摇倒计时 / 弹幕发数与命中扣血 / 渲染真画出 / 小弟上限 / 档位切换 / 冲撞速度 / 弹幕上限 / 520 只怪下 60fps）并接进 `tools/reg-site.ps1`；顺带修了档位判定的真 bug（正着遍历时第一档永远命中，Boss 会永远停在压制档） |
| 09-22 | 站点 | 《吸血鬼幸存者》**全站榜**（共创线：前端 + 后端代码）：云后端新增 `GET /api/score`（公开只读）与 `POST /api/score`（要云账号会话），只存「账号名 + 存活时间/击杀/等级/波次」，前 50 名、每人 60 秒一次、只留个人最好；游戏开始面板多了「🌍 全站榜」（看榜不用登录，上榜要云账号 —— GitHub-Gist 模式的存档不经过服务端，界面里明说了）。后端代码在游戏仓库 `tools/cf-worker.js`，**部署由站长执行**；契约测试 10/10（冷却/越界/同名去重/50 上限/限流），游戏侧探针 15/15。另加**中继兜底**：`*.workers.dev` 在部分网络被整段黑洞，于是 `functions/[[path]].js` 也转发 `/api/score`（用户 → pages.dev 边缘 → Worker，中间那跳在 Cloudflare 内网），前端按「中继优先、直连兜底」自动切换（探针 ⑦′ 专门验这条） |
| 09-22 | 游戏 | **M72 化学品转化链（前半）**：工业区搜到的化学品终于有用了 —— 据点医疗台把化学品做成抗生素/消毒剂，弹药台把手雷与 5.56 穿甲弹做出来（各有每日产能上限，升级台子 +1；跨天回满）。配方表是唯一真值（`src/v4/chem-core.ts`），并带两条「不许套利」的判据：按获取成本当量核对每行投入≥产出、再查有没有环。单测 842/842、探针 13/13（含材料守恒与「产能用完」禁用态），历史 15 支探针复跑全绿。后半「传闻口径」留待下一步 |
| 09-22 | 站点 | 《吸血鬼幸存者》**个人纪录榜（跨设备）**：存档追加 `top` 前 5 条，跟云存档同一份私有 Gist 走；开始面板显示「🏆 个人纪录」，结算面板显示「本局排进个人榜第 N 名」；合并口径是本机 ∪ 云端、按时间降序取前 5。顺带修了 `Save.load` 的一个隐坑：数组字段走 `isFinite` 判断时两个以上元素的数组会被整条丢掉。探针 14/14（含真打完一局）。另新增 `tools/reg-site.ps1`：一条命令跑完共创游戏页 + 游戏厅 + 帧率体检，结果落盘 |
| 09-22 | 站点 | 《吸血鬼幸存者》**面板态掉帧修复**：覆盖层 `#overlays` 的整屏 `backdrop-filter: blur(2px)` 叠在每帧重绘的画布上，合成器被迫每帧重模糊整屏 —— 面板态（升级三选一 / 暂停 / 结算 / 开始菜单）实测 32fps。改法：去掉这个 blur（改成略厚的径向渐变补回磨砂感）+ 面板打开时画布每 3 帧画一次（玩法本来就暂停了），面板态回到 60fps；结论写进了游戏目录的 `AGENT-CHANNEL.md` |
| 09-22 | 站点 | 《吸血鬼幸存者》接上**云存档**：直接复用游戏厅既有的账号库（`/games/account.js` → 私有 Gist `vampire-survivors__main.json`），开始面板多了「☁ 云存档」一行 —— 未登录安全置灰，登录后可上传/下载/双向同步，换设备登录同一个 GitHub 接着玩；纪录类字段两边取最高（下载不会冲掉本机成绩），每局结束自动推一份；CSP 只放行 `api.github.com` 与账号中继，不登录一个请求都不发。另加「📮 共创留言板」：两位共创者的 agent 通过 git 往 `js/data/notes.js` 追加留言，游戏里直接可读（约定见 `AGENT-CHANNEL.md`） |
| 09-22 | 站点 | **BETA 条不再压住游戏主画面**：条的高度写进 `:root` 的 `--beta-h`（含手机端那条提醒的高度，随换行/语言/窗口变化实时同步），全屏舞台类页面（游戏）用它把自己让到条下面 —— 《吸血鬼幸存者》的 `#app` 与「← 回游戏厅 / 🤝 共创」站点壳都跟着下移，画布高度正好等于视口减条高；顺带修掉回退文案不插值的 bug（没引 i18n 的页面会把「当前版本 {v}」原样显示出来） |
| 09-22 | 站点 | 《吸血鬼幸存者》**掉帧修复**：根因是每帧两次全屏填充（地面铺图 + 每帧新建的 radialGradient 暗角）叠在 2 倍缩放的画布上（像素 ×4），而 JS 只占 2% 时间、其余全是光栅化 —— 1440×900@2x 实测 p50 = 33ms（31fps）。改法：暗角/低血红屏预渲染成 1/4 小图每帧贴一次、贴图铺满整屏时省掉底色填充、渲染缩放按像素预算压回整数档（像素画不需要 4 倍像素）、不再每帧读 `clientWidth` 强制重排；同样 1440×900@2x 与 2560×1440@2x 实测都回到 60fps |
| 09-22 | 站点 | 游戏厅上新 **《吸血鬼幸存者》（与朋友共创）**：朋友做的自动攻击生存游戏，本站托管收录 —— 新增 `/games/vampire-survivors/`（带「← 回游戏厅 / 🤝 共创」站点壳与同款 BETA 声明条）、游戏厅卡片 + 鸣谢区「共创」一栏、中英双语词条；给这份第三方作品也加了 CSP（全自包含、零外联）。上线核验时发现线上 20 个脚本串行要 ≈15s（GitHub Pages 每请求 0.6~1.0s），改成 `defer` 并行加载（执行顺序不变），自检探针也从固定 `sleep` 改成等 `readyState=complete`；鸣谢区署名 **Alan.Liu**（主人的朋友） |
| 09-22 | 游戏 | **M70 藏身处经济重做**：新「♻️ 回收台」把通用材料换成建材（木料/铁片/布/胶带/化学品/汽油/芯片，每天有额度、工作台每级 +1 额度）；升级曲线 ×0.6 → ×0.35，发电机/无线电减芯片。满级全设施的材料成本从 ≈2170 AP（310 天）降到 ≈316 AP（45 天）—— 100 天的局终于建得起。审计与前后数字：游戏仓库 docs/BASE-REVIEW.md |
| 09-22 | 游戏 | **M69 感染链咬合**：带没清创的感染伤口过夜，全身感染值不再自然消退而是 +4/处；人体页可吃抗生素压住当晚（一天一次），感染到爆发期伤口也长不好 —— 伤病、环境与资源终于互相咬合 |

### 游戏同步史（本站只记"同步了哪个里程碑"，细节看游戏仓库）

- **M24 → M41**：卡片墙 / 账号体系 / 辐射与口径穿透 / 地图缩放与悬浮窗 / 存档加密 / 教程 / 彩蛋期同步（09-14 ~ 09-16）。
- **M42 → M59**：无尽模式、滚动与地图布局、商人买卖、教程沙盒六章、据点革新、速通堵漏、辐射症状、大区图抽搐修复（09-16 ~ 09-20）。
- **M60 → M71**：沙盒跨区硬验证、材料/等级缺口视觉、引诱器进教学、全量审计修 6 个 bug、存档不丢写（M65）、
  建筑内部地图（M66）、楼内行动日志（M67）、身体状态接进动作（M68）、感染链咬合（M69）、藏身处经济重做（M70）、商人好感度与解锁（M71）（09-19 ~ 09-22）。

## 相关链接

- 主页：<https://bobbychina.github.io/>
- 游戏厅：<https://bobbychina.github.io/games/>
- 《丧尸末日生存》：<https://bobbychina.github.io/games/zombie-survival/>
- 游戏仓库的 CHANGELOG：<https://github.com/Bobbychina/zombie-survival/blob/main/CHANGELOG.md>
- 自检脚本：`node tools/site-probe.mjs [url]`（渲染 + 截图 + 禁用词/链接/控制台错误检查）
